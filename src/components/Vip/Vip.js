import React from 'react';

const Vip = () => {
    return (
        <div>
            <h1>This is page generated using Logical AND. if user available you can see this page.</h1>
        </div>
    );
};

export default Vip;